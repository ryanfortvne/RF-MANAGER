
  # RF MANAGER

  This is a code bundle for RF MANAGER. The original project is available at https://www.figma.com/design/hRnmEyC3f8rEdb6dQpyPsI/RF-MANAGER.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  